import { PrismaClient } from '@prisma/client'
const prisma = new PrismaClient()

import {successCode,failCode,errorCode} from '../ultis/response.js'


const getAllTodo = async(res,req) =>{
    try {
        const data = await prisma.
    } catch (error) {
        
    }
}
